SELECT id, completed, description
	FROM public.tasks;
	delete from public.tasks where description='';
	commit;
	alter table public.tasks auto_increment = 3;  //MySql
	ALTER SEQUENCE tasks_id_seq RESTART WITH 13;  //Postgresql
